# 2026-09-09 编码器反馈纠正后复查

用户明确说明此前编码器反馈插错，并纠正连接。14:38:05 启动单台只读复查：错误码已为 0，CiA402 状态为 Switch on disabled，位置反馈恢复。连续 10 秒、11 组样本均无 Fault，55 次 SDO 上传全部成功。

| 项目 | 纠正前 | 纠正后 |
| --- | --- | --- |
| 从站 | 单台 Diamond，PREOP | 单台 Diamond，PREOP，身份匹配 |
| `6040` 控制字 | 0 | 0 |
| `6041` 状态字 | `0x0208`，Fault | `0x0240`，Switch on disabled |
| `603F` 错误码 | `0x738A` | 0 |
| `6064` 位置 | 固定为 0 | 观察范围 -506000556 至 -506000552 counts |
| `6060/6061` 模式 | 0 / 0 | 0 / 0 |
| PDO 分配与映射 | `1C12/1C13` 含第二个空索引 | 与纠正前逐项相同，问题仍在 |

本次仅运行 `scripts/watch_diagnostics.py`，没有发送故障复位、使能、模式、目标或 PDO 写入。结束后临时 `ec_master`、`ec_generic` 均已卸载。

在软件未调整 PDO/编码器参数的条件下，用户纠正插接后错误消失，强烈支持此前反馈连接错误与 `0x738A` 有关；不能再把该报警笼统归因于 IgH PDO 偏移或旧手册未覆盖的编码器类型 5。接线过程中是否伴随断电重启未单独记录，因此没有验证旧报警由哪一步清除，也没有据此证明此前 PREOP/SDO 复位有效。

位置读数为带符号的原始 counts，小范围变化表明反馈已不再固定为零。它不代表机器人已经回零、单位比例正确、运动方向正确或绝对坐标已标定。后续使能前必须以新鲜实际位置对齐目标，不能直接把目标设为 0。

剩余工作转向明确 PDO 初始化、禁止使能的周期通信与 DC 验证，以及完整实时运动后端、单位/零点、限位/急停、回零等。当前没有继续复位的必要，也没有进行运动验收。

原始输出：`build/encoder-reconnected-2026-09-09-143805.json` 和同名 `.log`。记录已与纠正前的 `mailbox-serialized-2026-09-09-142331.json` 对比，PDO 对象逐项相同；11 组样本均校验控制字为 0、错误码为 0、状态字掩码为 disabled。

关于前一条 XML 问题：两个仓库内现有 `PowerPMAC/Configuration/eni.xml` 是生成的网络配置（根为 `EncryptedEtherCATConfig`，内容可解析），已保存设备身份和实际使用的 PDO/DC 配置，IgH 离线配置正是从中提取。仓库内另外两个 `.devdesc.xml` 是 PMAC/CODESYS 设备描述；本轮在两个仓库内未发现以 `EtherCATInfo` 为根的厂家原始 ESI。ENI 内没有 `2100` 编码器参数对象或类型枚举描述，不能用它替代电机/编码器参数备份。
